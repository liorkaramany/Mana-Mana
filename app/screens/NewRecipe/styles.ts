import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  page: {
    flex: 1,
    paddingTop: 16,
    paddingHorizontal: 20,
  },
  title: {
    marginBottom: 16,
  },
  scrollView: {
    gap: 8,
  },
  uploadButton: {
    marginTop: 6,
    marginBottom: 8,
  },
});
