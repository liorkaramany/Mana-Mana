import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  page: {
    flex: 1,
    paddingTop: 16,
    paddingHorizontal: 16,
  },
  title: {
    marginBottom: 16,
  },
  uploadButton: {
    marginTop: 16,
    marginBottom: 8,
  },
});
